namespace AbioticEditor.App.Views.World;

/// <summary>World editor: summary chips + scrollable tab strip + one view per tab.</summary>
public partial class WorldEditorView : ContentView
{
    public WorldEditorView()
    {
        InitializeComponent();
    }
}
