namespace AbioticEditor.App.Views.Player;

/// <summary>Raw JSON tab: round-trip the player save through an external editor.</summary>
public partial class PlayerRawTab : ContentView
{
    public PlayerRawTab()
    {
        InitializeComponent();
    }
}
